services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORTAL_URL=http://portal:3000
      - SITE_DOMAIN=207.180.212.180.nip.io
      - CLIENT_URL=http://207.180.212.180.nip.io
      - WALRUS_CONTEXT=mainnet
      - WALRUS_EPOCHS=1
      - PORT=3000
      # These should be set via Coolify env vars (secrets):
      # - SESSION_SECRET=
      # - ENCRYPTION_KEY=
      # - ADMIN_MNEMONICS=
      # - GITHUB_CLIENT_ID=
      # - GITHUB_CLIENT_SECRET=
    volumes:
      - backend-data:/app/data
      - backend-builds:/app/builds
    depends_on:
      portal:
        condition: service_healthy
    restart: unless-stopped

  portal:
    image: mysten/walrus-sites-server-portal:mainnet-v2.6.0
    environment:
      - PORTAL_CONFIG=/etc/walrus-sites/portal-config.yaml
    volumes:
      - ./portal-config.yaml:/etc/walrus-sites/portal-config.yaml:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:3000 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

volumes:
  backend-data:
  backend-builds:
